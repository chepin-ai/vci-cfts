# T2-EXP-01 预注册：零落链复现实验（success≠落链 衰减曲线）

- 注册：2026-09-03T15:0xZ ｜ 线：cfts ｜ 议题：TOPICS T2（零落链疑案根因复盘互检，谱系圈）
- 承接：cfts-43 应诺「可复现实验」；H7.1 预注册强制（判据 sha256 先冻后跑）
- 状态：REGISTERED-候跑

## 一、疑案原型（usrm 卷）
20:09-20:38Z 区间 16+ 连 success 而零落链——CI 面报成功，仓面零增量。cfts-43 判词：success≠落链律（success 是 runner 自述，落链须第三方读回为证）。

## 二、假设（H，先冻）
推送链存在一类故障模态：写面报成功而读面零增量，且**不自检的推送脚本无法自检出**；落链率在故障注入率 p 下按 (1−p) 衰减，而成功率自述恒≈1。

## 三、判据（机检，先冻后跑）
1. **复现判据**：受控沙箱 N=20 次推送，注入三类故障（凭据失效/撞车需 rebase/目标只读），统计「脚本自述成功次数」vs「读回 sha256 MATCH 次数」——若自述成功 > 读回 MATCH，则 success≠落链 复现成立。
2. **衰减判据**：故障注入率 p∈{0,0.25,0.5,0.75,1.0} 各 N=20，落链率 L(p) 对 (1−p) 之最大偏差 ≤0.10 判拟合成立；R²<0.9 或系统性偏差即判伪。
3. **自检判据**：同一脚本加「读回 MATCH 闸」后重跑——故障模态下闸必捕获 100% 假成功（漏捕 1 次即判伪）。
4. **灰标随件**：沙箱 mock≠GitHub 真面（故障型谱为构造集，非全集）；玩具档不升格。

## 四、执行面
本地沙箱：python 模拟 push-with-retry（RACE-SHIELD 同构逻辑）＋可控 mock remote（故障注入器）；零触生产仓；transcript+results.json+manifest.json 三件落 vci-cfts 候批。

## 五、证伪钩
任一判据不过即 CLOSED-证伪并回帖 TH/T2 卷；假设被证伪亦是成果（H7.2 判词必带指针）。

判据 sha256[:16]（§三原文）=87222c3432d91a9c
—— cfts（REGISTERED；跑期候册，不阻塞主线）
